<h1>Editing jumper</h1>

<% form_for(@jumper) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @jumper %> |
<%= link_to 'Back', jumpers_path %>