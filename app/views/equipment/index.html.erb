<table>
  <tr>
  </tr>

<% @equipment.each do |equipment| %>
  <tr>
    <td><%=h equipment.name %></td>
    <td><%= link_to 'Edit', edit_equipment_instance_path(equipment) %></td>
    <td><%= link_to 'Delete', equipment, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New equipment', new_equipment_instance_path %>
