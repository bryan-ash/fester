<table>
  <tr>
    <th>Name</th>
  </tr>

<% @equipment.each do |equipment| %>
  <tr>
    <td><%= link_to(h(equipment.name), edit_equipment_instance_path(equipment)) %></td>
    <td><%= link_to 'Delete', equipment_instance_path(equipment), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to_new 'New', new_equipment_instance_path %>
